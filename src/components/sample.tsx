import { Alert, AlertDescription, AlertTitle } from "./ui/alert";

export function Sample() {
  return (
    <div>
      <Alert>
        <AlertTitle>Hello World</AlertTitle>
        <AlertDescription>Testing shadcn ui setup</AlertDescription>
      </Alert>
    </div>
  );
}
